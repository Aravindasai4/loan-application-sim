Implement “Decision Contract v0.3 – Drift & Stability” in this Flask app (main.py + SQLite). Do NOT change the app’s overall structure. Keep existing routes working.

Goals:
1) Add data needed for drift/stability monitoring
2) Add new dashboard sections for drift + alert signals
3) Add JSON endpoints to export metrics

Constraints:
- SQLite only (same DB file)
- No external services
- No fancy frontend frameworks; simple HTML ok
- Migrations must be backward compatible (ALTER TABLE / create new tables)
- All timestamps stored in UTC ISO8601

A) Database changes
1) Ensure decisions table has:
- created_at (exists)
- source (manual/sim)
- sim_day (nullable)
- annual_income, loan_amount, credit_score, employment_years (if not already stored)
If any are missing, add via ALTER TABLE with nullable defaults.

2) Create a new table: daily_metrics
Columns:
- day TEXT PRIMARY KEY (YYYY-MM-DD, UTC)
- total INTEGER
- approve INTEGER
- review INTEGER
- reject INTEGER
- avg_risk REAL
- max_risk REAL
- review_rate REAL
- avg_income REAL
- avg_loan REAL
- avg_credit REAL
- avg_emp_years REAL
- reason_counts_json TEXT  (JSON dict {code: count})
- updated_at TEXT

3) Create a new table: alerts
Columns:
- id INTEGER PRIMARY KEY AUTOINCREMENT
- created_at TEXT
- day TEXT
- severity TEXT (INFO/WARN/CRITICAL)
- alert_type TEXT (e.g., REVIEW_RATE_SPIKE, RISK_SPIKE, CREDIT_SHIFT, INCOME_SHIFT)
- message TEXT
- details_json TEXT

B) Background computation (no cron needed)
Add a function compute_daily_metrics_and_alerts(conn, lookback_days=30) that:
1) Aggregates decisions by day (UTC date from created_at)
2) For each day, computes counts and averages and reason_counts_json:
   - For reasons, parse explanation_json / reasons_json field (whatever exists) and count reason "code"
3) Upserts into daily_metrics
4) Computes drift signals by comparing:
   - “today” vs 7-day trailing baseline (previous 7 days, excluding today)
Signals to compute:
- review_rate_spike: today_review_rate > baseline_review_rate + 0.10 AND today_total >= 10 => WARN
- avg_risk_spike: today_avg_risk > baseline_avg_risk + 0.15 AND today_total >= 10 => WARN
- credit_shift: abs(today_avg_credit - baseline_avg_credit) >= 40 AND today_total >= 10 => WARN
- income_shift: abs(today_avg_income - baseline_avg_income)/baseline_avg_income >= 0.25 AND today_total >= 10 => WARN
If baseline missing, skip alerts.

Insert alerts rows when triggered (avoid duplicates per day+type; if already exists, don’t reinsert).

Call compute_daily_metrics_and_alerts() at the end of:
- /apply POST
- /simulate route (or wherever simulation happens)
- human override action (approve/reject)

C) New routes
1) GET /metrics/daily.json
Return last 30 days from daily_metrics as JSON list sorted by day asc.

2) GET /alerts
HTML page listing latest 50 alerts (new nav link “Alerts”).
Include: created_at, day, severity (pill), type, message, link to dashboard.

3) GET /alerts.json
Return latest 200 alerts as JSON list.

D) Dashboard updates (HTML)
Extend the existing /dashboard page with two new sections:

1) “Drift (last 14 days)”
Table with day, total, review_rate, avg_risk, avg_credit, avg_income, and flags column that shows:
- “review spike” if alert exists that day
- “risk spike” if alert exists that day
- “credit shift” if alert exists that day
- “income shift” if alert exists that day

2) “Alerts (recent)”
Show the latest 10 alerts with severity + message.

E) Keep it safe + debuggable
- Wrap JSON parsing in try/except; on failure, treat as empty reasons list.
- Add basic error logging to console with traceback for compute function exceptions.
- Do not break production gunicorn start.

Deliverables:
- Updated main.py with DB migrations, compute function, routes, dashboard changes, and alerts page.
- Any new HTML strings needed.
- Confirm by running locally and ensuring /dashboard, /alerts, /metrics/daily.json work.
